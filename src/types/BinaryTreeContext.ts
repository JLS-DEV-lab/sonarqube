import { TreeNode } from "@types";

export type BinaryTreeContext = {
    /* Refs */

    /* State */
    binaryTree: TreeNode;
    setBinaryTree: (binaryTree: TreeNode) => void;

    /* API Keys */

    /* Form data */
};